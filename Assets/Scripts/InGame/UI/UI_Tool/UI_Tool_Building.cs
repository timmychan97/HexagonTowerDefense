using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UI_Tool_Building : UI_Tool_GameUnit
{
    // public Building building;
    
    // public void SetAction() 
    // {
    //     base.SetAction(() => {
    //         if (GameController.INSTANCE.IsGamePaused()) return;
    //         if (Tile.active.CanPlaceUnit() && GameController.INSTANCE.CanBuyGameUnit(building))
    //         {
    //             Building b = Instantiate(building);
    //             TileManager.INSTANCE.SetTileContent(b.gameObject);
    //             GameController.INSTANCE.OnBuyGameUnit(b);
    //         }
    //     });
    // }
}
